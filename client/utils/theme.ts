import {createTheme} from "@mui/material/styles";

export const theme = createTheme({
    palette: {
        primary: {
            main: '#2322c0',
            contrastText: '#000000',
        },
        secondary: {
            main: '#fff',
            contrastText: '#FFFFFF',
        }
    }
})
